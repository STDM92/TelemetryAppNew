import React from "react";
import { WidgetFrame } from "../../components/WidgetFrame";

export function PitInfoWidget() {
  return (
    <WidgetFrame title="Standings">
      <div className="placeholder-copy">
        Shared pit info widget placeholder. Later this becomes a shared space for pit infos about the supported driver and other drivers, as well as a place to update pit strategy for the driver.
      </div>
    </WidgetFrame>
  );
}
