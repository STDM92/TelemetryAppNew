import React from "react";
import { WidgetFrame } from "../../components/WidgetFrame";

export function StandingsInfoWidget() {
  return (
    <WidgetFrame title="Standings">
      <div className="placeholder-copy">
        Shared standings widget placeholder. Later this becomes a shared space for standings. (Relative, Overall, Class Standings, etc.)
      </div>
    </WidgetFrame>
  );
}
